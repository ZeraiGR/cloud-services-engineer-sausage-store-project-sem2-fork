---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-secret
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
type: Opaque
stringData:
  DB_URI: "mongodb://{{ .Values.global.mongodb.reports_user.username }}:{{ .Values.global.mongodb.reports_user.password }}@mongodb:{{ .Values.global.mongodb.containerPort }}/{{ .Values.global.mongodb.reports_user.database }}?authSource={{ .Values.global.mongodb.reports_user.database }}"